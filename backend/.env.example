PORT=3001
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins for cross-origin requests
# Use '*' for all origins (development only) or specify exact origins for production
# Examples:
#   CORS_ORIGINS=http://localhost:3000,https://nestera.io,https://app.nestera.io
#   CORS_ORIGINS=*  (allows all origins - DO NOT USE in production)
CORS_ORIGINS=*

# Stellar Network
STELLAR_NETWORK=testnet
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
HORIZON_URL=https://horizon-testnet.stellar.org
STELLAR_WEBHOOK_SECRET=your_webhook_secret_here
STELLAR_EVENT_POLL_INTERVAL=10000

# Soroban Testnet – RPC Fallback URLs (comma-separated, in priority order)
# Uncomment and populate with backup nodes to enable automatic failover.
SOROBAN_RPC_FALLBACK_URLS=https://rpc-backup1.stellar.org,https://rpc-backup2.stellar.org
HORIZON_FALLBACK_URLS=https://horizon-backup1.stellar.org,https://horizon-backup2.stellar.org

# RPC Retry Configuration
RPC_MAX_RETRIES=3
RPC_RETRY_DELAY=1000
RPC_TIMEOUT=10000

# Contract
CONTRACT_ID=YOUR_DEPLOYED_CONTRACT_ID
GOVERNANCE_EXECUTOR_SECRET_KEY=SXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
GOVERNANCE_EXECUTION_CONTRACT_ID=YOUR_GOVERNANCE_EXECUTION_CONTRACT_ID
GOVERNANCE_TREASURY_CONTRACT_ID=YOUR_TREASURY_CONTRACT_ID
GOVERNANCE_RATE_CHANGE_METHOD=set_rate
GOVERNANCE_TREASURY_TRANSFER_METHOD=transfer
GOVERNANCE_PAUSE_METHOD=pause
GOVERNANCE_UNPAUSE_METHOD=unpause

# ── Database ──────────────────────────────────────────────────────────────────
# Option A – URL-based (takes precedence; typical for cloud/container deployments)
DATABASE_URL=postgresql://user:password@localhost:5432/nestera

# Option B – Host-based (used when DATABASE_URL is absent; useful for Kubernetes secrets)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=nestera
# DB_USER=user
# DB_PASS=password

# JWT
JWT_SECRET=your_super_secret_key_here
JWT_EXPIRATION=1h
JWT_REFRESH_EXPIRATION=7d

# Redis (optional)
REDIS_URL=redis://localhost:6379

# Mail (SMTP)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=your_email@example.com
MAIL_PASS=your_email_password
MAIL_FROM="Nestera" <noreply@nestera.io>
# Hospital Integration
HOSPITAL_1_ENDPOINT=https://api.hospital1.com
HOSPITAL_2_ENDPOINT=https://api.hospital2.com
HOSPITAL_3_ENDPOINT=https://api.hospital3.com
HOSPITAL_MAX_RETRIES=3
HOSPITAL_RETRY_DELAY=1000
HOSPITAL_REQUEST_TIMEOUT=10000
HOSPITAL_CIRCUIT_BREAKER_THRESHOLD=5
HOSPITAL_CIRCUIT_BREAKER_TIMEOUT=60000

# ── Database Backup ───────────────────────────────────────────────────────────
BACKUP_S3_BUCKET=nestera-db-backups
BACKUP_S3_REGION=us-east-1
BACKUP_AWS_ACCESS_KEY_ID=your_aws_access_key_id
BACKUP_AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# 64 hex characters = 32-byte AES-256 key. Generate with: openssl rand -hex 32
BACKUP_ENCRYPTION_KEY=your_64_char_hex_encryption_key_here_replace_this_value_now
BACKUP_RETENTION_DAYS=30
BACKUP_TMP_DIR=/tmp
